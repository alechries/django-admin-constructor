# django-admin-constructor
A Django admin module for building and managing models, relation, and admin interface structure with minimal code changes - all configurable through the Django admin panel.
